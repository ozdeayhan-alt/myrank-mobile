/** Window coordinates from measureInWindow (avatar / vote buttons). */
export type ProfileVoteFountainAnchor = {
  avatarWindowX: number;
  avatarWindowY: number;
  avatarSize: number;
  contentWidth: number;
  voteRowWindowY: number;
  /** Yükselt pulse merkezi */
  upPulseWindowX: number;
  upPulseWindowY: number;
  /** Alçalt pulse merkezi */
  downPulseWindowX: number;
  downPulseWindowY: number;
};
