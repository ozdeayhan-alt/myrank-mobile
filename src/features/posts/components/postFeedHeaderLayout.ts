/** Puan pill içeriği: label (12) + score (20) + dikey padding (8). */
export const FEED_HEADER_BLOCK_HEIGHT = 40;

/** @deprecated Use FEED_HEADER_BLOCK_HEIGHT */
export const FEED_SCORE_PILL_MIN_HEIGHT = FEED_HEADER_BLOCK_HEIGHT;

/** Profil avatar — sağ üst Puan bloğu ile aynı yükseklik. */
export const FEED_AVATAR_SIZE = FEED_HEADER_BLOCK_HEIGHT;
